import React from 'react';

const name = (props) => {
    return();
}
export default name;

import React, {Component} from 'react';

class name extends Comment {
    render(
        
    );
}
export default name;