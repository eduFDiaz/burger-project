import React from 'react';

const name = (props) => (
    <div></div>
)
export default name;

import React, {Component} from 'react';

class name extends Comment {
    render(
        
    );
}
export default name;